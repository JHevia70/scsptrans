name: Create Release

on:
  push:
    paths:
      - 'Data/Localization/spanish_(spain)/global.ini'
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get date
        id: meta
        run: |
          DATE=$(TZ='Europe/Madrid' date +'%d/%m/%Y %H:%M')
          TAG=$(TZ='Europe/Madrid' date +'%Y%m%d%H%M%S')
          echo "tag=v${TAG}" >> $GITHUB_OUTPUT
          echo "name=Traduccion SC ${DATE}" >> $GITHUB_OUTPUT
          FILENAME="Traduccion_SC_$(TZ='Europe/Madrid' date +'%Y%m%d_%H%M').ini"
          echo "filename=${FILENAME}" >> $GITHUB_OUTPUT

      - name: Copy with descriptive name
        run: cp "Data/Localization/spanish_(spain)/global.ini" "${{ steps.meta.outputs.filename }}"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: ${{ steps.meta.outputs.name }}
          files: ${{ steps.meta.outputs.filename }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
